# --- Core framework ---
Flask==3.0.3
Flask-Cors==4.0.1
Werkzeug==3.0.3

# --- Environment + utilities ---
python-dotenv==1.0.1
requests-oauthlib==1.3.1

# --- Database ---
psycopg2-binary==2.9.9     # PostgreSQL driver (used on Render/Neon)
sqlite3-binary; sys_platform == "win32"  # (optional: already built-in)
SQLAlchemy==2.0.36          # only if you still keep db_switch.py (safe to leave)

# --- Password hashing & security ---
bcrypt==4.1.3
itsdangerous==2.2.0

# --- Email / SMTP ---
smtplib3==0.0.1             # optional: built-in smtplib covers most needs
email-validator==2.2.0

# --- AI / Groq API ---
groq==0.4.2                 # latest Groq client

# --- Gunicorn for production (Render) ---
gunicorn==21.2.0
