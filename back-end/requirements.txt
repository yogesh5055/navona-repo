Flask>=3.0,<4              # Flask web framework
flask-cors>=4.0,<5         # Enable CORS (for frontend ↔ backend API calls)
requests-oauthlib>=1.3.1,<2 # Google OAuth login support
python-dotenv>=1.0,<2      # Load .env files locally
groq>=0.11,<1              # For your AI roadmap generation feature
gunicorn>=21,<22           # Production web server (Render)
reportlab>=4.2,<5          # For generating PDFs (optional feature)
psycopg2-binary==2.9.9     # PostgreSQL driver (for Neon / Render)
